# Repo Face State

This folder stores birth and continuity state for `Eve`.
Do not treat these files as arbitrary editable projections. Persistent memory should cross typed operation boundaries.

`docs/eve-face.md` is the human-readable mission/memory surface for Eve's repo
Face. Keep `.voidbot/state/eve.cc` as the typed authority, and keep the docs
surface aligned when Eve's durable mission changes.
